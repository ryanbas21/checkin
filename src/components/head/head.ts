import * as React from 'react';
import Head from 'next/head';

const Header: React.SFC<void> = () =>
  <Head>
    <title>A Scrum Manager</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </Head>;

export default Header;
